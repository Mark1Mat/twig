{% extends 'base.html.twig' %}

{% for article in articles %}
    <h1>{{ article.title }}</h1>
{% endfor %}





{#

 {% if kenny.sick %}
    Kenny is sick.
{% elseif kenny.dead %}
    You killed Kenny! You bastard!!!
{% else %}
    Kenny looks okay --- so far
{% endif %}

{% for article in articles %}
    <h1>{{ article.title }}</h1>
{% endfor %}

#}